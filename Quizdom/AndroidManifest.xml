<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.breanawiggins.quizdom"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- Splash Screen -->
        <activity
            android:name="com.breanawiggins.quizdom.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name="com.breanawiggins.quizdom.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity android:name="com.breanawiggins.quizdom.SignUp" >
        </activity>
        <activity android:name="com.breanawiggins.quizdom.Login" >
        </activity>
        <activity android:name="com.breanawiggins.quizdom.HomeScreen" >
        </activity>
        <activity
            android:name="com.breanawiggins.quizdom.FriendsListActivity"
            android:label="@string/title_activity_friends_list" >
        </activity>
    </application>

</manifest>
